---
  - name: Git clone nginx
    become: yes
    git:
      repo: 'https://github.com/GvickyGithub/docker-nginx.git'
      dest: /data/nginx
      update: yes
      force: yes
    ignore_errors: yes
